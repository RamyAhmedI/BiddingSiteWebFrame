{% extends 'base.html.twig' %}

{% block title %}ShipmentDetails{% endblock %}

{% block body %}
    <h1>ShipmentDetails</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shipment_detail.id }}</td>
            </tr>
            <tr>
                <th>ProductName</th>
                <td>{{ shipment_detail.productName }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ shipment_detail.price }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ shipment_detail.address }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_shipment_details_index') }}">back to list</a>

    <a href="{{ path('app_shipment_details_edit', {'id': shipment_detail.id}) }}">edit</a>

    {{ include('shipment_details/_delete_form.html.twig') }}
{% endblock %}
