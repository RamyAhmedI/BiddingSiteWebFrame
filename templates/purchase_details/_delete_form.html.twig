<form method="post" action="{{ path('app_purchase_details_delete', {'id': purchase_detail.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ purchase_detail.id) }}">
    <button class="btn">Delete</button>
</form>
